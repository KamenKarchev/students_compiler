program {
    factorial(int n) -> int {
        if (n <= 1) { return 1; }
        return n * @factorial(n - 1);
    }

    main() -> void {
        int n = 5;
        print("n =", n, "fact(rec) =", @factorial(n));
    }
}
