program {
    add(int a, int b) -> int {
        return a + b;
    }

    main() -> void {
        int x = 10;
        int y = 20;
        int sum = @add(x, y);
        print("Sum of", x, "and", y, "is", sum);
    }
}
